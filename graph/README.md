# graph/

Runtime database location. SparrowDB writes the embedded firm graph here at
runtime (e.g. `graph/firm.db`, `graph/audit.db`). Database files are
git-ignored; only this README and `.gitkeep` are tracked.
