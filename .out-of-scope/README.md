# Out-of-Scope Knowledge Base

This directory stores persistent records of rejected feature requests (enhancements closed as `wontfix`).
One file per concept (kebab-case), not per issue. See `docs/agents/OUT-OF-SCOPE.md` for the full convention.
