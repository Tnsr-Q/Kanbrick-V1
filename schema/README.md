# schema/

Ontology definitions for the firm graph. Phase 1 adds the runtime ontology
(Person / Company / Segment) and Phase 4 adds `code.ontology.json` for the
graphify-rs code graph that is unified into SparrowDB.
